version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: [ "java" ,"-jar", "app.jar" ]
    entrypoint: /usr/bin/entrypoint.sh
    ports:
      - "8100:8100"